#!/usr/bin/env zsh

brew-essentials() {
    brew install cmake
    brew install coreutils
    brew install ctags
    brew install git
    brew install p7zip
    brew install pstree
    brew install pyenv
    brew install sqlite
    brew install ssh-copy-id
    brew install the_silver_searcher
    brew install tmux
    brew install vim
    brew install watch
    brew install zsh
}
